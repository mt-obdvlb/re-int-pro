# 测试与验收

2026-09-08：后端29项pytest通过；覆盖契约正常/错误、幂等、队列/领取、取消、deadline、失租恢复、分页、脱敏、导出失败、六策略、正常/缺失观测、工具白名单、费用预留/恢复/对账、供应商Mock错误、快照冻结、候选历史、统计配对完整性。前端1项交互测试覆盖不确定请求重试仍用原幂等键和原正文。命令见 [README](../README.md)。

Apifox项目8800905/main现维护20个用例、45条断言，均以本地真实后端runner通过。接口ID/用例ID/报告哈希见 [资源索引](apifox/resource-index.json)；schema validate和get回读不计为HTTP通过。

| API用例 | 覆盖 |
| --- | --- |
| 01–05 | health、任务列表、不存在任务、六策略、创建真实快照运行 |
| 06–09 | 缺字段、探测上限越界、非法策略、列表分页 |
| 10–14 | 不存在运行/取消、非法事件游标、不存在证据/报告 |
| 15 | 总预算500元、准入不超过450元（实际操作限额默认1元） |
| 16–20 | 真实任务、完成运行、事件、证据、定位报告正向回读 |

错误码分别为INCIDENT_NOT_FOUND和RUN_NOT_FOUND；创建fixture为inc_1875fd2c391740aa/competitive_cost。正向回读用fixture_run_id环境变量，必须对应完成的FakeLLM连接池运行。原规划16–24的编号未落地，现16–20已按上述真实资源分配；幂等冲突/取消/费用并发等内部边界由pytest覆盖，不能冒称Apifox已经执行了原24项。

浏览器验证实际新建、评分、D/c/U展开、报告、取消→刷新保留终态和390px表单；截图对照见 [完整验收](full-validation.md)。MockTransport验证供应商错误注入，真正百炼20开发例另记，二者不可混称。

API实现直接暴露仓库规范作为/openapi.json，因此两个JSON相等不证明实现合规；pytest检查实际路由集合和响应schema，Apifox检查真实HTTP断言。当前没有独立实现规范自动推导/全面语义diff。外部付费主实验、完整干净机器重采集、生产并发压测不在已通过范围。
