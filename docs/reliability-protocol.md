# 预测与停止可靠性补强约定

2026-09-09，在0bde31d之后、下一次真实调用之前记录。保留上一批原始结果，开发集仍为原20任务；不读取测试集调参，不重新采集有利案例。

## 干预

1. 修正模型缺少服务执行语义的问题。根据lab/service.py提供所有任务相同的静态服务说明：worker使用独立连接池；checkout先访问缓存和数据库、入队、在队列超过阈值时等待，之后才校验配置；配置无效只产生拒绝标志与日志，并不强制增加延迟。只给结构与代码语义，不给注入参数、任务标签或观测答案。提示版本proposal-v3，旧提示保留，冻结版本决定实际使用哪份提示。
2. competitive-v4定位前必须检查最终候选全部非unknown预测；缺失或失败的观测不能算验证通过，矛盾仍直接反驳。两个异常通道、分差和未知故障限制不变。单候选时优先补足通道，再核对剩余明确预测；旧v2/v3运行保留原停止行为。完整检查不保证因果或真实根因，只保证不存在尚未检查的明确预测。

## 对照与报告

先用既有20份真实初始候选，对照前缀停止/完整检查，隔离停止条件的效果；报告正确、未定位、探测数与漏查反驳，允许正确数下降，不伪装成改进。

再按相同固定模型、温度、开发任务，完整运行一批proposal-v3/competitive-v4，保存初始候选及用量；只运行一次，不挑选成功的重复。仍共用原SQLite费用库、累计1元上限，不提高限额。预计额外约0.05–0.10元，模型失败或预算限制照常留行。

对新旧两份候选缓存分别做两种停止条件回放，形成“提示×停止条件”四格比较。同缓存内归因停止逻辑；提示间比较同时受一次模型采样差异影响，只作为开发证据。必须报告候选覆盖、非unknown预测覆盖与一致率，不能仅通过大量改填unknown提高一致率。

若新提示和完整检查未改善可靠性，照实保留；不放宽成功条件追求漂亮数字。规则基线16/16仍为参照，不能因为补上下文就宣称LLM必要性成立。成本下降不是本轮目标。旧文档的“执行前拒绝”按当前源码纠正，不修改实验服务来迎合解释。
