# 项目协作规范

## 项目定位

本仓库用于计算机考研与复旦复试面试准备。用户已选择 T1：基于竞争假设与探测成本的故障诊断 Agent（ProbeOps）。目标是具备可验证的机制改进、工程深度和真实面试证据。

默认中文。已确定 React + TypeScript + Vite 前端、Python FastAPI 后端；Agent 用 Python 自写，禁止引入 Agent 框架。LLM 使用阿里云百炼，现有 `.env` 中 `BAILIAN_API` 仅后端读取。logging 和 tracing 必须覆盖正常、异常、取消和恢复路径。详见 `docs/architecture.md`。

## 当前阶段与批准边界

以 `docs/project-plan.md` 中的阶段记录为工作入口，以用户的明确指令为最终依据。

1. 初始化仓库和必要文档。
2. 用户批准第 1 步后，调研并提供候选题目。
3. 用户批准第 2 步后，由用户确定题目。
4. 题目确定后，再与用户确定后续步骤。

用户于 2026-09-05 已明确选择 T1 并授权实施前整体规划、文档和 Apifox 设计维护；2026-09-06 明确要求“真正搭建项目”，授权 P1。当前交付 P1 FakeLLM 基础链路；P2 环境与观测仍待用户批准。各阶段见 `docs/implementation-plan.md`，每关仍需验收。

每一步完成后停止推进，向用户报告交付物、验证结果和待批准事项。未获批准，不进入下一步。提交或推送的授权不等于进入下一阶段的授权。P0 不提前编写应用骨架或开展付费模型实验。

## 选题与证据要求

以下是第 2 步获批后的工作要求，不构成提前开始调研的授权。

- 围绕明确的用户问题说明为什么需要 Agent，以及其决策、工具使用、反馈与终止机制。
- 对每个候选题目说明 AI 亮点、技术难点、面试可讲述内容、实现边界与主要风险。
- 创新主张必须与已有论文或开源项目比较；区分待验证假设与已经证实的结果。
- 理论依据优先引用论文和官方文档，记录来源、检索日期及其如何支持具体机制。
- 给出可获得的数据或任务、基线和评价方法，考虑正确性、成本、延迟及失败处理。
- 不编造论文、实验结果或招生要求；当前招生事实需要另外查询官方来源。
- 模型训练、付费资源、部署与数据使用等具体实施范围在选题后确定。

## 文件与实现规则

- 修改前读取相关文件、Git 状态与差异；保留用户已有变更。
- 只修改当前步骤需要的文件，不顺便重构或引入依赖。
- 当前运行目录为 backend、frontend、scripts，使用根目录 uv.lock 与 frontend/pnpm-lock.yaml；CI 仅运行 FakeLLM。
- 安装、运行和验证命令见 README；P2 前不引入实验容器或真实故障工具。
- 不提交密钥、令牌、真实本地环境配置、私有数据或含个人信息的材料。`.gitignore` 不能替代提交前检查。

## 选题后的工程约束

- Python 依赖用 uv、前端用 pnpm；实际搭建时锁版本，不写不存在的运行命令。
- 总投入目标280小时、上限300小时；API预算不超过500元，准入450元，金额账本持久化。当前不消费模型。
- Agent 仅使用四种只读观测工具；故障注入与真值评分在独立实验控制器，不将隐藏标签暴露给Agent。
- `docs/api/openapi.json` 是唯一接口契约源；Apifox项目8800905、main、Git Spec模式，连接 `mt-obdvlb/re-int-pro`。先改规范、同步生成、CLI回读；不要直接修改生成接口或换成同名项目。
- API用例通过Apifox CLI维护：先help/schema/validate，再create/update，最后get回读；runner报告才证明真实请求通过。不得将schema校验或Mock成功表述为后端验证。
- 所有日志、trace、异常与导出必须脱敏；不打印 `.env`、完整项目设置或连接配置。
- 历史候选调研与预演稿保留；本轮之后的实施约束优先看requirements/architecture/agent-design/evaluation-plan，不把旧供应商报价当当前预算。

## 验证与 Git

- 文档变更检查内容一致性、相对链接、阶段记录和 `git diff --check`。
- 代码变更执行与变更直接相关的最小充分验证；无法验证时明确说明原因，不声称通过。
- 阶段交付达到要求后创建清晰的本地提交。延续简单的提交风格，优先采用 Conventional Commits。
- 用户已授权合理安排推送，可在验证后推送当前阶段成果；推送后核对远程分支提交。
- 暂存具体文件或当前任务的差异块，禁止用 `git add .` 或 `git add -A` 混入无关改动。
- 不强制推送、不重写历史、不清理或覆盖用户未授权删除的文件。
- 交付时报告相关文件、验证结果、提交短哈希、推送状态，并等待下一阶段批准。
